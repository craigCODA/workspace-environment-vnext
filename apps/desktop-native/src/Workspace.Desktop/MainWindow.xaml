<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Workspace.Desktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Workspace Environment"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="Workspace Environment">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <Grid Grid.Row="1" Background="#26373B">
            <WebView2 x:Name="WorkspaceWebView" DefaultBackgroundColor="#26373B" />
            <Border x:Name="StartupStatus"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="24"
                    Padding="14,9"
                    Background="#CC0E171A"
                    CornerRadius="10">
                <TextBlock x:Name="StartupStatusText"
                           Foreground="#FFF0EEE6"
                           Text="Starting Coda…" />
            </Border>
        </Grid>
    </Grid>
</Window>
