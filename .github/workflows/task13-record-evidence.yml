name: task13-record-evidence

on:
  push:
    branches:
      - task13/execution
    paths:
      - .github/workflows/task13-record-evidence.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  record:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Sanity
        run: echo evidence-recorder-ready
